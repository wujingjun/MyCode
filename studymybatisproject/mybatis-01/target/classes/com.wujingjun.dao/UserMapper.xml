<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wujingjun.dao.UserMapper">

    <select id="findAllUser" resultType="com.wujingjun.pojo.User">
    select * from mybatisuser
    </select>

    <select id="findUserById" parameterType="int" resultType="com.wujingjun.pojo.User">
        select * from mybatisuser where id=#{id}
    </select>

    <insert id="addUser" parameterType="com.wujingjun.pojo.User">
        insert into mybatisuser (name , password) values (#{name},#{password});
    </insert>

    <update id="updateUser" parameterType="com.wujingjun.pojo.User">
        update mybatisuser set name = #{name},password=#{password} where id=#{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from mybatisuser where id=#{id}
    </delete>
</mapper>